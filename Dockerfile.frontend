FROM node:20-alpine

WORKDIR /app

# Copy only package files to leverage caching when possible
COPY package*.json ./

RUN npm install

# Copy source (will be overridden by volume in docker-compose during dev)
COPY . .

EXPOSE 3001

CMD ["npm", "run", "dev", "--", "-p", "3001", "-H", "0.0.0.0"]
